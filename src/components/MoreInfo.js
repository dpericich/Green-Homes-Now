import React from 'react';
import styled from 'styled-components';

const MoreInfo = () => {
    return(
        <div>
            I am the MoreInfo Item
        </div>
    )
};

export default MoreInfo;