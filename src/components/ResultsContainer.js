import React from "react";
import styled from "styled-components";

const ResultsContainer = () => {
    return(
        <div>I am the Results Container</div>
    )
};

export default ResultsContainer;