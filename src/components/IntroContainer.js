import React from 'react';
import styled from 'styled-components';

const IntroContainer = () => {
    return(
        <div>
            I am the intro container
        </div>
    )
};

export default IntroContainer;